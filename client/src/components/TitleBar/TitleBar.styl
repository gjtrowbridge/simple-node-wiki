
.title-bar {
  border: solid standard-border background-color;
  border-bottom-width:(standard-border/2);
  input {
    width: 100%;
    height: 100%;
    font-weight: bold;
    font-size: 2em;
    text-align: center;
  }
}
